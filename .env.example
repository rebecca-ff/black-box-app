# Anthropic API key — used SERVER-SIDE ONLY by /api/brief.
# Never prefix with NEXT_PUBLIC_ and never commit the real value.
# Set it locally in .env.local, and in Vercel: Project → Settings → Environment Variables.
ANTHROPIC_API_KEY=
